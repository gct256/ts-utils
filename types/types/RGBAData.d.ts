/** RGB-based color data type. */
export declare type RGBAData = {
    /** Red value. 0 <= n <= 255, integer. */
    readonly red: number;
    /** Green value. 0 <= n <= 255, integer. */
    readonly green: number;
    /** Blue value. 0 <= n <= 255, integer. */
    readonly blue: number;
    /** Alpha value. 0 <= n <= 255, integer. */
    readonly alpha: number;
};
